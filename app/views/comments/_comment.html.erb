<div id = "comment_<%= comment.id %>" class="comment">
    <p>Creator: <%= comment.creator %></p>
    <p>Contents: <%= comment.body %></p>
    <%= link_to "Update", edit_square_discussion_comment_path(@square, @discussion, comment)%> | 
    <%= link_to "View Comment", square_discussion_comment_path(@square, @discussion, comment) %> | 
    <%= link_to "Delete", square_discussion_comment_path(@square, @discussion, comment), method: :delete, data: {confirm:"Do you want to delete this comment?"} %>
</div>